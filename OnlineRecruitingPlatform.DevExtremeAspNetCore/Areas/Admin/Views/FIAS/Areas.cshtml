@using FIASApi.Model.Entities
<div class="mb-3 card">
    <div class="card-header">Районы</div>
    @(Html.DevExtreme().DataGrid<VArea>()
        .ID("gridContainer")
        .ShowBorders(true)
        .ShowRowLines(true)
        .ColumnHidingEnabled(true)
        .FilterPanel(f => f.Visible(true))
        .HeaderFilter(headerFilter => headerFilter.Visible(true))
        .FilterRow(f => f.Visible(true))
        .Columns(columns =>
        {
            columns.AddFor(a => a.Regionname).Caption("Наименование региона");
            columns.AddFor(a => a.Offname).Caption("Наименование района");
            columns.AddFor(a => a.Socrname).Caption("Тип объекта");
            columns.Add()
                .Type(GridCommandColumnType.Buttons)
                .Width(50)
                .Buttons(b =>
                {
                    b.Add()
                        .Hint("Детали")
                        .Icon("fas fa-info")
                        .Text("Информация")
                        .OnClick("onDetailsAreaIconClick");
                });
        })
        .Paging(paging => paging.PageSize(100))
        .Pager(pager =>
        {
            pager.ShowPageSizeSelector(true);
            pager.AllowedPageSizes(new[] { 10, 25, 50, 100 });
            pager.ShowInfo(true);
        })
        .KeyExpr("Aoguid")
        .DataSource(ViewBag.areas))
</div>